import { Category } from "./category";

export class Product {
    _id: string;
    name: string;
    category: any;
    price: number;
    imageUrl: string;
    image: any;
    productId: string;
    description: string;
}